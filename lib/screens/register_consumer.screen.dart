import 'package:flutter/material.dart';

class RegisterConsumerScreen extends StatelessWidget {
  const RegisterConsumerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
