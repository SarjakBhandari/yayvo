import 'package:flutter/material.dart';

class RegisterRetailerScreen extends StatelessWidget {
  const RegisterRetailerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
